# 🫁 سامانه هوشمند تشخیص بیماری‌های ریوی (COVID-19 & Pneumonia)

پروژه نهایی درس هوش مصنوعی - دانشگاه صنعتی خواجه نصیرالدین طوسی 

**استاد راهنما:** خانم دکتر پیشگو

**ناظر پروژه:** مهندس علیرضا قربانی 

---

## 📋 معرفی پروژه

این پروژه با هدف طراحی و پیاده‌سازی یک سیستم تشخیص خودکار بیماری‌های ریوی از جمله **کووید-۱۹**، **پنومونی ویروسی** و **کدورت ریوی** بر اساس تصاویر رادیولوژی قفسه سینه (CXR) توسعه یافته است. در این مسیر از تکنیک‌های پیشرفته یادگیری عمیق و انتقال دانش (Transfer Learning) برای دستیابی به بالاترین دقت استفاده شده است.

---

## 🛠 پیش‌نیازهای نصب و اجرا

برای اجرای صحیح پروژه، ابتدا کتابخانه‌های مورد نیاز را نصب کنید. پیشنهاد می‌شود از یک محیط مجازی (Virtual Environment) استفاده کنید.

```bash
# کلون کردن پروژه
git clone https://github.com/your-repo/ai-lung-detection.git
cd ai-lung-detection

# نصب کتابخانه‌های مورد نیاز
pip install numpy pandas matplotlib seaborn scikit-learn tensorflow keras streamlit pillow plotly

```

---

## 📂 ساختار درختی پروژه (Project Structure)

ساختار فایل‌ها به گونه‌ای طراحی شده است که جریان داده (Data Flow) و مدیریت مدل‌ها به صورت بهینه انجام شود:

```text
.
├── data/
│   ├── raw/                # دیتاست اصلی (COVID-19 Radiography Database)
│   │   ├── COVID/          # ۳۶۱۶ تصویر
│   │   ├── Lung_Opacity/   # ۶۰۱۲ تصویر
│   │   ├── Normal/         # ۱۰۱۹۲ تصویر
│   │   └── Viral_Pneumonia/# ۱۳۴۵ تصویر
[cite_start]│   └── processed/          # تانسورهای ذخیره شده (.npy) برای سرعت بارگذاری [cite: 2]
├── notebooks/
│   ├── Phase1_Analysis.ipynb  # تحلیل EDA و پیش‌پردازش
│   └── Phase2_Training.ipynb  # آموزش مدل و بهینه‌سازی
├── models/
│   └── final_model.h5      # مدل نهایی آموزش دیده
├── results/
│   └── charts/             # نمودارهای ارزیابی و ماتریس اغتشاش
[cite_start]├── app.py                  # رابط کاربری (Streamlit Dashboard) [cite: 3]
└── README.md

```

---

## 🚀 شرح فازهای پروژه

### فاز ۱: تحلیل اکتشافی (EDA) و آماده‌سازی

در این فاز، تمرکز بر درک عمیق دیتاست و رفع چالش‌های آماری بود:

* 
**تحلیل توازن کلاس‌ها:** شناسایی ناترازی شدید داده‌ها (تعداد زیاد تصاویر Normal در مقابل Viral Pneumonia).


* 
**پیش‌پردازش:** تغییر ابعاد تمامی تصاویر به  و نرمال‌سازی مقادیر پیکسل‌ها.


* 
**استراتژی ارزیابی:** به دلیل عدم توازن، به جای دقت (Accuracy) محض، بر معیارهای Precision و Recall تمرکز شد.



### فاز ۲: مدل‌سازی و یادگیری انتقالی

در این مرحله از معماری‌های پیش‌آموزش‌دیده برای استخراج ویژگی استفاده شد:

* **انتخاب مدل:** مقایسه بین VGG16، ResNet50 و **DenseNet121**. مدل DenseNet121 به دلیل "اتصال متراکم" لایه‌ها، به عنوان استاندارد طلایی برای تصاویر رادیولوژی انتخاب شد.


* **افزایش هوشمند داده (Augmentation):** اعمال چرخش و تغییر کنتراست برای شبیه‌سازی شرایط واقعی دستگاه‌های رادیولوژی.
* 
*نکته فنی:* از چرخش عمودی (Vertical Flip) به دلیل حفظ ساختار آناتومیک ریه پرهیز شد.




* **فرمول بهینه‌سازی:** استفاده از تابع هزینه `Categorical Crossentropy` برای طبقه‌بندی چندکلاسه:



---

## 💻 دمو و رابط کاربری (UI)

رابط کاربری این پروژه با استفاده از فریم‌ورک **Streamlit** طراحی شده است تا پزشکان و کاربران بتوانند به راحتی با مدل تعامل داشته باشند.

**ویژگی‌های پنل هوشمند:**

* 
**Dashboard:** نمایش آمار کلی دیتاست و نمودارهای توزیع به صورت تعاملی.


* 
**Image Analysis:** امکان بارگذاری تصویر رادیولوژی (PNG/JPG) و دریافت تشخیص آنی مدل.


* 
**Visual Feedback:** نمایش تصویر بارگذاری شده در کنار نتیجه تشخیص با استفاده از کارت‌های رنگی برای نمایش وضعیت بیماری.



---

## 📊 تحلیل نتایج

مدل نهایی با بهره‌گیری از تکنیک **Fine-tuning** (آزادسازی لایه‌های انتهایی استخراج ویژگی) توانست به دقت بالایی در تشخیص کلاس‌های بحرانی (مانند COVID) دست یابد. استفاده از `Class Weights` در فاز آموزش، تاثیر ناترازی داده‌ها را به حداقل رساند.

---

این پروژه تحت قوانین اخلاق پژوهشی و با استفاده از منابع Open-Source توسعه یافته است.
